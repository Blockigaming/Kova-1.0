{
  "name": "kova-model",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "validate:data": "node scripts/validate-data.mjs",
    "validate:stack": "node scripts/validate-stack.mjs",
    "preflight": "npm run validate:data && npm run validate:stack",
    "test": "node --test tests/*.test.mjs"
  }
}
